# 核心依赖
transformers>=4.45.0
datasets>=2.20.0
peft>=0.12.0
accelerate>=0.34.0
modelscope>=1.18.0
swanlab
gradio>=4.0.0
pandas

# 外挂数据库
chromadb>=0.5.0
sentence-transformers>=3.0.0

# ⚠️ AMD ROCm 专用 PyTorch（请用以下命令安装，不要直接 pip install torch）
# pip install torch torchvision --index-url https://download.pytorch.org/whl/rocm6.2
torch>=2.4.0
torchvision

# vLLM（部署用，训练时不需要）
# pip install vllm --break-system-packages
